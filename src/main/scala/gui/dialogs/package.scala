package gui.dialogs

/**
 * Package object for dialogs to provide convenient imports
 */
package object admin {
  // Admin dialogs are accessible directly
}

package object auth {
  // Authentication dialogs are accessible directly  
}

package object common {
  // Common dialogs are accessible directly
}

package object features {
  // Feature dialogs are accessible through sub-packages
}
